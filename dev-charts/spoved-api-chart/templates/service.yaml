apiVersion: v1
kind: Service
metadata:
  name: {{ include "spoved-api-chart.fullname" . }}
  labels:
    {{- include "spoved-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spoved-api-chart.selectorLabels" . | nindent 4 }}
